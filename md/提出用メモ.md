# 提出用メモ

課題番号：2

① 該当：あり

② 場所：ItemRepository.java > findByName()

③ 検索結果が0件のとき null を返していた。  
List を返すメソッドは null ではなく空リストを返すべきなので、  
null チェックと return null の分岐を削除し、  
template.query() の戻り値（仕様上0件でも空リスト）をそのまま返すよう修正した。



---



課題番号：8
 
① 該当：なし
 
② 場所：—
 
③ —



---



課題番号：3

① 該当：なし

② 場所：—

③ —



---



課題番号：19
① 該当：あり

② 場所：OrderService.java > paymentMethodJudge()

③ Integer型の`==`比較によりオートボクシング問題が発生する可能性があった。
  　定数側から`.equals()`で比較する`Integer.valueOf(1).equals(order.getPaymentMethod())`に修正した。
  　これによりnullが返ってきた場合のNPEも防止できる。
  


---



課題番号：24

① 該当：なし

② 場所：—

③ —



---



課題番号：39

① 該当：あり

② 場所：ItemRepository.java > showItemDetail()

③ `queryForObject()`は0件のとき`EmptyResultDataAccessException`が発生するため、  
存在しない商品IDが入力された場合にページが壊れるリスクがあった。  
0件でも空リストを返す`query()`に変更し、`result.isEmpty()`で0件チェックをする形に修正した。



---



課題番号：23

① 該当：あり

② 場所：
- OrderRepository.java > orderLoad()
- OrderRepository.java > findByOrdertable()
- User.java > toString()

③ 注文履歴・注文詳細の表示処理でパスワードを含む全カラムを取得していた。  
パスワードは表示処理に不要なため、SQLから`u.password AS u_password`を削除し、  
`ORDER_RESULTSET`のマッピングからも`user.setPassword()`を削除した。  
また`User.java`の`toString()`でpasswordを`*****`でマスク化した。



---



課題番号：35

① 該当：あり

② 場所：item_detail.html > appendMessage()

③ `innerHTML`にユーザー入力を埋め込んでいたためXSSリスクがあった。  
既存の`escapeHtml()`では一部エスケープ漏れがあり、将来的なリスクも残っていた。  
`textContent`に変更することで入力値をテキストとして扱い、XSSリスクを根本的に排除した。  
改行は`white-space: pre-wrap`で対応し、不要になった`escapeHtml()`はコメントアウトした。



---



課題番号：9

① 該当：あり

② 場所：OrderForm.java > getTimestamp()

③ `e.printStackTrace()`はコンソールにしか出力されず本番ログに残らないため、  
`log.error()`に置き換えた。また`System.out.println`も同様に削除した。  
ロガーのクラス指定が`OrderRepository.class`と間違っていたため`OrderForm.class`に修正した。  
`return null`はNPEのリスクがあるため`IllegalArgumentException`を投げる形に変更した。



---



課題番号：30

① 該当：あり

② 場所：
- OrderRepository.java > orderLoad()
- OrderRepository.java > findByOrdertable()

③ `RIGHT JOIN`と`LEFT JOIN`が混在しており可読性が低く、  
`RIGHT JOIN order_items`とすることで孤立した`order_items`が結果に混入するリスクがあった。  
`orders`を基準にデータを取得するという意図を明確にするため、  
すべての結合を`LEFT JOIN`に統一した。



---



課題番号：16

① 該当：あり

② 場所：
- CartService.java > findItemsByCartId()
- CartToppingRepository.java > findByCartId()（新規追加）

③ カートアイテムの件数分だけSQLが発行されるN+1問題が発生していた。  
`CartToppingRepository`に`findByCartId()`を新規追加し、  
全トッピングを1回のSQLでまとめて取得した上でJava側（Stream）で振り分けることで、  
SQL実行回数を合計2回に削減した。



---



課題番号：34

① 該当：あり

② 場所：item_deail.js > isLargeTopping の比較箇所

③ `==`（等価演算子）を使うと型強制が起きるため意図しない型変換のリスクがあった。  
`===`（厳密等価演算子）に変更することで型も含めて厳密に比較できるようにした。



---



課題番号：29

① 該当：あり

② 場所：ItemRepository.java > findByName()

③ `SELECT *`を使うと不必要なカラムまで取得しパフォーマンスの無駄やセキュリティリスクがあった。  
一覧表示に不要な`description`を除外し必要なカラムのみを指定したSQLに変更した。  
また`description`なしの`ITEM_LIST_ROW_MAPPER`を新規作成し`findByName()`専用に使うようにした。



---



課題番号：18

① 該当：あり

② 場所：CartService.java > フィールドインジェクション箇所

③ `@Autowired`によるフィールドインジェクションはテスト時にモックを注入しにくく、  
依存しているクラスがどこなのかもわかりにくかった。  
`@Autowired`を削除しフィールドを`final`にした上でコンストラクタを追加し、  
コンストラクタインジェクションに変更した。



---



課題番号：27

① 該当：あり

② 場所：OrderService.java > sendMail()

③ `getInputStream()`で取得したストリームが明示的に閉じられておらず、  
ファイルハンドルが開きっぱなしになりリソースリークが発生するリスクがあった。  
`try-with-resources`を使いtryブロックを抜けると自動的に`close()`が呼ばれるようにした。  
これにより例外発生時も含め確実にリソースを解放できるようになった。



---



課題番号：33

① 該当：なし

② 場所：—

③ —



---



課題番号：20

① 該当：あり

② 場所：Order.java > getOrderItemList()

③ 内部リストの参照をそのまま返していたため、呼び出し側から内部状態を自由に書き換えられカプセル化が破綻していた。  
`Collections.unmodifiableList()`を使って変更不可のリストを返すようにし、  
nullチェックを追加して`orderItemList`がnullの場合は`Collections.emptyList()`を返すようにした。



---



課題番号：21

① 該当：あり

② 場所：
- Order.java・OrderItem.java・OrderTopping.java・Item.java
- Topping.java・User.java・Cart.java・CartItem.java・CartTopping.java

③ `equals()`と`hashCode()`が未実装のため同じIDを持つオブジェクトでも別物として扱われ、  
`Set`での重複検出や`Map`のキー検索が正しく動作しなかった。  
`id`フィールドのみを基準に両メソッドをセットで実装した。



---



課題番号：28

① 該当：あり

② 場所：
- CartController.java > inCart()
- ItemService.java > findAllTopping()
- CurryApplication.java
- application.yml

③ `application.getAttribute()`は`Object`型を返すため`(List<Topping>)`キャストの型保証がなく、  
誤った型が入っていた場合に実行時`ClassCastException`が発生するリスクがあった。  
`@Cacheable`を使いSpringが型安全なキャッシュを管理する形に変更し、  
`CartController`では`itemService.findAllTopping()`を直接呼ぶ形に修正した。



---



課題番号：37

① 該当：なし

② 場所：—

③ —



---



課題番号：40

① 該当：あり

② 場所：CartService.java > getToppingIndex()

③ 選択トッピング数×全トッピング数のネストループでO(n²)になっており、  
データ量が増えると処理時間が二乗で急増するリスクがあった。  
事前に`Map<Integer, Topping>`を作成しトッピングIDをキーにすることで、  
ループ内の取り出しをO(1)で行えるようにしO(n²)からO(n)に改善した。



---



課題番号：1

① 該当：あり

② 場所：
- Order.java > statusフィールド
- OrderService.java > paymentMethodJudge()
- OrderRepository.java > ORDER_RESULTSET内statusマッピング
- OrderStatus.java（新規作成）

③ `status`フィールドが`Integer`型のためマジックナンバー問題と不正な値が入るリスクがあった。  
`OrderStatus` enumを新規作成し型安全な状態管理に変更した。



---



課題番号：4

① 該当：あり

② 場所：
- Order.java > Builder（setter削減、引数なしコンストラクタ削除）
- OrderControler.java > orderCompletion()
- OrderService.java > order()
- OrderRepositoryTest.java / OrderServiceTest.java（連鎖修正）

③ Builderパターン導入時にsetterと引数なしコンストラクタを残しており、build()の必須チェックを迂回できる状態だった。DB自動採番・段階的組み立てが必要なsetId()/setUser()/setOrderItemList()のみ残し、他は全て削除。呼び出し元でstatus・userIdをBuilderに含める形に変更し、影響を受けたテストコードも修正した。



---



課題番号：22

① 該当：なし

② 場所：—

③ —



---



課題番号：15

① 該当：あり

② 場所：sql/Migration/3_add_foreign_keys.sql（新規作成）

③ 外部キー制約がないと`order_items`削除時に`order_toppings`が孤立レコードとして残り、  
DBレベルでデータの整合性が保証されなかった。  
`ON DELETE CASCADE`付きの外部キー制約を追加することで親レコード削除時に  
子レコードも自動削除されDBレベルで整合性を保証できるようにした。



---



課題番号：31

① 該当：あり

② 場所：sql/Migration/4_add_check_constraints.sql（新規作成）

③ DBレベルで有効な値を制限していないため管理ツールや他のアプリから  
不正な値が直接挿入できてしまう問題があった。  
CHECK制約を追加してDBとJava両方で制約する多層防御の設計にした。



---



課題番号：13
 
① 該当：あり（機能追加）
 
② 場所：
- AddressItemForm.java・AddressForm.java（新規作成）
- AddressController.java（新規作成）
- address_form.html・address_confirm.html（新規作成）
- address_form.js・address_form.css（新規作成）
③ 固定行しか選択できず複数件の住所を一度に登録できなかった。  
`createElement`・`appendChild`を使って行を動的に追加・削除できるフォームを新規実装した。  
`addresses[0].name`形式のname属性でSpring Boot側でリスト形式として受け取れるようにした。



---



課題番号：10

① 該当：なし

② 場所：—

③ —



---



課題番号：38

① 該当：なし

② 場所：—

③ —



---



課題番号：11

① 該当：あり

② 場所：CartService.java > calcTotal()

③ forループで合計を計算する場合、中間変数が必要で処理の意図が読み取りにくかった。  
`mapToInt().sum()`を使うことで意図が一目でわかり、コードが簡潔になった。



---



課題番号：36
 
① 該当：あり（#13と同一ファイルで実装）
 
② 場所：address_form.js
 
③ 削除ボタンそれぞれにリスナーを登録すると重複登録になりメモリリークの原因になる。  
イベント委譲を使い親要素（`#addressContainer`）に1つだけリスナーを登録した。  
クリックイベントがバブリングで親まで伝播するため動的に追加した行にも対応できる。



---



以下、上級課題



---



課題番号：5

① 該当：なし

② 場所：—

③ 実際のコードでは在庫の確認と減算が`UPDATE ... WHERE stock_amount >= :amount`という1本のSQLで実装されており、TOCTOU問題が構造的に起こらない設計になっていたため該当なし。課題文コードを用いて、楽観的ロック（versionカラムによる競合検知）の仕組みと、その適用範囲（競合が稀なケースに向く）を概念学習として確認した。



---



課題番号：6

① 該当：あり

② 場所：
- OrderService.java > paymentMethodJudge()
- PaymentMethod.java（新規）/ PaymentProcessor.java（新規）
- CashOnDeliveryPaymentProcessor.java（新規）/ CreditCardPaymentProcessor.java（新規）

③ if-elseによる2択判定では支払い方法の追加のたびに既存コードを書き換える必要があり、誤編集のリスクがあった。Strategyパターンを導入し、支払い方法ごとに専用クラス（PaymentProcessor実装）を作成。OrderServiceはMap検索で該当処理を呼び出すだけの共通ロジックのみを持つ形にし、新しい支払い方法の追加時にOrderServiceを一切変更しなくてよい設計にした。あわせてOrderServiceをコンストラクタインジェクション化（課題18対応）。



---



課題番号：26

① 該当：なし

② 場所：—

③ 課題文コードでは支払い方法ごとに別クラス（CreditCardOrderService／CodOrderService）を用意し、共通処理（バリデーション・在庫チェック・ステータス更新・メール送信）を重複して実装していた。修正漏れによる処理順序のズレが起きるリスクがある。Template Methodパターンで共通の流れをfinalメソッドとして親クラスにまとめ、決済処理のみサブクラスに委ねる設計を概念学習として確認した。実プロジェクトでは注文処理が単一のメソッドに統一されており該当なし。



---



課題番号：7

① 該当：あり

② 場所：
- OrderControler.java > orderCompletion()
- OrderService.java > order()
- OrderCompletedEvent.java（新規）
- OrderMailListener.java（新規）

③ order()とsendMail()が別々に呼ばれており、呼び出し順序を守る責任が呼び出し元（OrderControler）に押し付けられていた。呼び忘れると注文は成立するがメールが送られない不具合につながる。Observerパターン（Spring ApplicationEvent）を導入し、OrderServiceは注文完了イベントを発行するだけにし、メール送信の実処理はOrderMailListenerに委譲する設計に変更。OrderControlerの直接sendMail()呼び出しは二重送信防止のため削除した。
（以下追記・レビュー対応）
OrderMailListener.onOrderCompleted()内でtry-catchし、例外を上位（OrderService.order()）へ伝播させずログにのみ記録する形に修正した。
これによりOrderServiceは引き続き「イベントを発行するだけ」で完結し、Observerパターンの疎結合な設計を維持したまま、注文の確定とメール送信の成否を分離できた。




---



課題番号：17

① 該当：あり

② 場所：
- CartController.java > inCart()
- CartItem.java > from()（新規）
- OrderService.java > insertOrderItem()
- OrderItem.java > from()（新規）

③ BeanUtils.copyProperties()はフィールド名が一致していれば意味が違っても機械的にコピーしてしまい、form.id（商品ID）がcartItem.id（主キー）に誤ってコピーされる問題があった。setId(null)による後付けの打ち消しで運用回避していた。CartItem.from()・OrderItem.from()という静的ファクトリーメソッドを追加し、必要なフィールドだけを明示的に指定して組み立てる設計に変更。同様のパターンだったOrderItem側（OrderService.insertOrderItem()）にも対応した。



---



課題番号：25

① 該当：なし

② 場所：—

③ リトライ・ログ機能の追加要件が現状なく、sendMail()内にもそれらを詰め込んで肥大化している箇所は存在しないため、Decoratorパターンの適用（MailServiceインターフェース化）は見送った。将来リトライ機能等の追加要件が発生した際に、改めて検討した方がいいと判断。



---



課題番号：12

① 該当：あり

② 場所：
- OrderForm.java > destinationZipcode / destinationTel
- InsertForm.java > zipcode / telephone
- PhoneNumber.java / PhoneNumberValidator.java（新規）
- PostalCode.java / PostalCodeValidator.java（新規）

③ 電話番号・郵便番号の正規表現がOrderForm・InsertFormの両方に重複しており、修正漏れによる画面間の不整合リスクがあった。Bean Validationのカスタムアノテーション（@PhoneNumber・@PostalCode）とConstraintValidator実装を新規作成し、正規表現の実体をValidator側の1箇所に集約。各フォームはアノテーションを貼るだけで済む設計に変更した。



---



課題番号：14

① 該当：あり

② 場所：
- orders.user_id
- order_items.order_id
- order_toppings.order_item_id

③ 外部キーとしてJOIN・WHERE条件に使われるカラム（orders.user_id、order_items.order_id、order_toppings.order_item_id）にインデックスが設定されておらず、フルテーブルスキャンが発生する状態だった。CREATE INDEX文を作成し、sql/Migration/5_add_indexes.sqlとして追加した。



---



課題番号：32

① 該当：あり

② 場所：
- ItemRepository.java > findByName()

③ 商品名の部分一致検索（LIKE '%name%'）が中間一致のため、B-treeインデックスが機能せずフルテーブルスキャンになる問題があった。pg_trgm拡張機能によるトライグラムインデックス（GIN）を追加し、中間一致検索でもインデックスが機能する構成に変更。sql/Migration/6_add_trgm_index.sqlとして追加した。



---
